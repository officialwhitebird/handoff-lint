# Claims-to-Evidence Mapping: handoff-lint v0.1.0

This mapping acts as a verification checklist to ensure all public claims regarding handoff-lint v0.1.0 are backed by source code evidence or CLI execution outputs.

| Public Claim | Allowed Wording | Supporting Evidence | Source File / Command | Risk if Overstated | Safer Fallback Wording |
| --- | --- | --- | --- | --- | --- |
| Local-first / Offline | Runs entirely locally and offline. | The package imports only standard libraries and PyYAML. No http/requests modules. | `handoff_lint/parser.py`, `handoff_lint/cli.py` | Users might assume there are zero package dependencies. | Runs locally as a CLI tool using Python and PyYAML. |
| Read-only | Does not modify, write, or delete workspace files. | Target file is opened with mode 'r' only. | `handoff_lint/parser.py` | Users might expect auto-fixing or formatting capabilities. | Evaluates target instructions in a read-only manner. |
| No telemetry | No usage statistics or crash reporting is collected. | Source code inspection shows zero telemetry/analytic hooks or endpoints. | Codebase under `handoff_lint/` | Custom third-party scripts could introduce network calls. | The core CLI contains no data collection hooks. |
| Deterministic findings | Findings are generated by a predictable rule set. | Rule validation is written with deterministic condition checks, no LLM. | `handoff_lint/validator.py` | Expectation of a smart AI reading sentence quality. | Performs basic static syntax and metadata key verification. |
| Checks Markdown + YAML | Validates frontmatter key-values and body headers. | Checks for start fence '---', parses keys, and searches for heading lines. | `handoff_lint/parser.py`, `handoff_lint/validator.py` | Users assuming it checks markdown styling, list syntax, or links. | Validates required frontmatter keys and Context/Acceptance Criteria headings. |
| Emits JSON output | Formatted JSON output available via CLI flag. | Supports --json option and writes a structured dictionary to stdout. | `handoff_lint/cli.py` | Assuming malformed YAML parse errors also output standard JSON. | Emits validation findings in JSON. Operational errors print to stderr. |
| Strict warning handling | --strict option elevates warnings to exit code 1. | CLI sets exit code to 1 if warnings are present under strict flag. | `handoff_lint/cli.py` | Confusing structural warnings with fatal syntax execution errors. | Treats empty scope or approval lists as validation failures under --strict. |
| Experimental / Prototype | Experimental tool designed as a portfolio prototype. | Documented status and prototype disclaimer in the README. | `README.md` | Users deploying it inside critical production pipeline stages. | Experimental prototype; not validated for broad production use. |
